{
  "name": "api-qa-automation-gate",
  "version": "1.0.0",
  "description": "API QA Automation Pipeline Portfolio Piece",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}